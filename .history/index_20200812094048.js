
// Write your code here
const num1 = 31;
const multiply = () => {
    num1 *= 2;
};

const random = () => {
    return Math.floor(Math.random()*10)+1;
};

const mod = (mod) => {
    return (mod%4);
};

const max = (set) => {
   return Math.max(set);
}