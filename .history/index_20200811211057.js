
// Write your code here

let multiply = (num1,num2) => {
    return (num1 * num2);
};

let random = () => {
    return Math.floor(Math.random()*10)+1;
};

let mod = (mod) => {
    return (mod%4);
};

let max = (set) => {
   return Math.max(set);
}